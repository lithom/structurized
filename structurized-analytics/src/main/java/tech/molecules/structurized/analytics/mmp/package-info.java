/**
 * PRISM-backed MMP analytics.
 *
 * <p>This package separates endpoint-independent structural MMP universes from
 * endpoint-specific numeric statistics. The SQLite repository is a reference
 * implementation intended for local validation and as a narrow SQL boundary for
 * later database-specific implementations.</p>
 */
package tech.molecules.structurized.analytics.mmp;
